import { UserRequest } from '../../src/@types/UserRequest';

describe('User Request', () => {

    it('should return email syntax error', () => {
        const user = new UserRequest();
        
    })

})
